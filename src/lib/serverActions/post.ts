'use server'

import { db } from '@/lib/db'

export const createPost = async () => {
  console.log('create Post action')
  // todo: revalidate topic page
  // todo: homepage revalidation at interval
}
